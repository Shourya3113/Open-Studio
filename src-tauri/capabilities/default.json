{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for Open Studio main window",
  "windows": ["main*"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
